# 最終課題 提出版設計書（じすい日和）

更新日: 2026-07-29

## 1. 課題概要

「じすい日和」は、自炊を習慣化するための記録アプリである。ごはんの写真と
メモを日々記録し、カレンダーやアルバムで振り返ることを中心機能とする。
お気に入りレシピ、食材の期限管理、公開を選んだ投稿のコミュニティ表示、
いいねにも対応する。

実装時点の技術構成は以下のとおり。

- フロントエンド: React 19、React Router 7、Axios
- バックエンド: FastAPI、SQLAlchemy、Pydantic v2
- 認証: JWT、bcrypt
- データベース: SQLite（ローカル）、PostgreSQL（本番）
- マイグレーション: Alembic
- デプロイ: Railway

## 2. 設計で重視する考え方

- MVPとして必要な機能を優先する
- ユーザー入力の表記ゆれや不正値を機械側で制御する
- ログイン確認とデータ所有者確認を分けて考える
- 設計判断を変えた場合は、実装だけでなく設計書にも理由を残す
- 保存先や実行環境など、設計判断が成立する前提条件を確認する

## 3. 機能とPhase

| 機能 | 対応データ | Phase | 状態・判断理由 |
|---|---|---|---|
| ユーザー登録・ログイン | users | Phase1 | 実装済み。MVPで必須 |
| 自炊投稿 | posts | Phase1 | 実装済み。アプリの中心機能 |
| 写真保存 | photos | Phase1 | 実装済み。投稿削除後も写真は保管庫に残す |
| カレンダー表示 | posts.cooked_date | Phase1 | 実装済み。作った日を基準に表示 |
| アルバム表示 | photos / posts | Phase1 | 実装済み。作った日で月別表示 |
| お気に入りレシピ | favorite_recipes | Phase2 | 実装時に前倒し |
| 賞味期限管理 | ingredients | Phase2 | 実装時に前倒し |
| いいね | likes | Phase3 | 実装時に前倒し。DB制約で二重いいねを防止 |
| 投稿の共有 | posts.is_public | Phase3 | 実装済み。明示的に公開を選択した投稿のみ共有 |
| デザインアンロック | unlocked_items | Phase3 | 未実装 |

## 4. 認証と所有者確認

パスワードは生値では保存せず、bcryptでハッシュ化した `hashed_password` のみを
保存する。JWT署名鍵はソースコードへ書かず、`JWT_SECRET_KEY` 環境変数から読む。

投稿、写真、お気に入りレシピ、食材の取得・更新・削除では、対象IDだけでなく
`user_id == current_user.id` を条件に含める。これにより、ログイン済みユーザーが
他人のIDを指定してデータへアクセスすることを防ぐ。

## 5. 投稿と「作った日」

### 判断

カレンダーは投稿操作をした日時ではなく、実際に料理を作った日で管理する。
そのため `posts.cooked_date` をDate型、NULL不可で保持する。

### 実装状況

2026-07-29の修正で実装済み。投稿作成時の初期値は当日で、投稿フォームから
変更できる。投稿一覧、コミュニティ、詳細、アルバムは `cooked_date` を基準に
表示し、`created_at` へのフォールバックは置かない。

## 6. 写真の設計

### 6-1. 写真保管庫

`photos` はユーザーごとの写真保管庫として扱う。投稿を削除した場合、写真自体は
削除せず `photos.post_id` をNULLへ戻す。写真を明示的に削除した場合のみ、DB行と
保存された実ファイルを削除する。

### 6-2. 投稿との関連付け

設計時は、同じ写真を複数投稿で利用できるように `cooking_post_photos` 中間テーブルを
最終案としていた。実装では、現在のMVPが「1投稿につき写真1枚」を前提としているため、
`photos.post_id` の直接FKを採用した。

投稿作成APIの `photo_id` で写真を関連付けることで、専用エンドポイントを増やさず
一度の投稿作成処理にまとめられる。この変更は実装漏れではなく、MVPを小さく保つための
再判断である。

複数写真または同一写真の複数投稿利用が必要になった場合は、中間テーブル方式を
再検討する。Phase2の判断期限は2026-08-31とする。

### 6-3. 形式と保存名

`photos.content_type` を保持し、JPEG、PNG、WebPのみを許可する。サイズ上限は5MBとし、
保存ファイル名にはユーザー入力を使わずUUIDを使用する。`photo_url` はOSに依存しない
スラッシュ区切りの相対パスで保存する。これらは2026-07-29の修正で実装済み。

### 6-4. 本番の保存先

画像本体をDBへ保存せず、DBにはパスだけを保存する判断は維持する。ただしRailwayの
通常ファイルシステムは永続化されないため、本番ではRailway Volume、S3、Cloudflare R2
などの永続ストレージを使用する。この外部設定の期限は2026-08-31とする。

## 7. 食材・期限管理

### memo

設計時はMVP優先度が低いとして削除予定だったが、実装して使用した結果、食材の状態や
用途を補足するために有用と判断した。そのため `ingredients.memo` は維持する。
これは実装しながら判断を変えた項目である。

### quantity

単位が「個」「本」「ml」「kg」のように異なるため、追加する場合はString型とする。
現時点では未実装であり、Phase2の実装判断期限を2026-08-31とする。

### storage_place

「冷蔵」「冷凍」「常温」の選択式とし、自由入力による表記ゆれを防ぐ。現時点では
未実装であり、Phase2の実装判断期限を2026-08-31とする。

### best_before

期限比較を行えるようDate型で保持する。文字列の自由入力にはしない。

## 8. 公開範囲とコミュニティ

投稿の公開はユーザーの明示的な意思を必要とする。`posts.is_public` はBoolean型、
NULL不可、初期値Falseとする。

コミュニティでは、他ユーザーの投稿のうち `is_public == True` のものだけを返す。
写真も公開投稿とJOINできるものだけを返し、投稿に紐づいていない保管庫内の写真や
非公開投稿の写真は返さない。

## 9. 削除時のデータ整合性

- 投稿削除時、その投稿へのいいねはカスケード削除する
- 投稿削除時、写真は削除せず `post_id` をNULLへ戻す
- 写真削除時、DB行と実ファイルの両方を削除する
- SQLiteでは `PRAGMA foreign_keys=ON` を有効にする
- 投稿更新時は `updated_at` を更新する

## 10. API一覧

### 認証

| Method | Path | 内容 |
|---|---|---|
| POST | `/api/auth/signup` | 新規登録 |
| POST | `/api/auth/login` | ログイン |
| GET | `/api/auth/me` | ログインユーザー取得 |

### 投稿・写真

| Method | Path | 内容 |
|---|---|---|
| POST | `/api/posts` | 投稿作成。`photo_id` で写真を関連付ける |
| GET | `/api/posts` | 自分の投稿一覧 |
| GET | `/api/posts/community/all` | 公開された他ユーザーの投稿一覧 |
| GET | `/api/posts/{post_id}` | 自分の投稿詳細 |
| PUT | `/api/posts/{post_id}` | 自分の投稿更新 |
| DELETE | `/api/posts/{post_id}` | 自分の投稿削除 |
| POST | `/api/photos` | 写真保存 |
| GET | `/api/photos` | 自分の写真一覧 |
| GET | `/api/photos/community` | 公開投稿に紐づく他ユーザーの写真 |
| DELETE | `/api/photos/{photo_id}` | 自分の写真削除 |

専用の `POST /api/posts/{post_id}/photos` は設けない。投稿作成時の `photo_id` に
統合したほうが、現在の1投稿1写真というMVPに対して単純だからである。

### カレンダー

専用の `GET /api/calendar` は設けない。現在のデータ量では `GET /api/posts` の結果を
フロントエンドで年月日ごとに絞り込むほうが単純である。投稿数の増加により
ページネーションや期間指定が必要になった時点で、専用APIを再検討する。

### その他

| Method | Path | 内容 |
|---|---|---|
| GET/POST/PUT/DELETE | `/api/favorites` | お気に入りレシピ管理 |
| GET/POST/PUT/DELETE | `/api/ingredients` | 食材と期限の管理 |
| GET | `/api/likes?post_ids=...` | 複数投稿のいいね数を一括取得 |
| GET/POST/DELETE | `/api/likes/{post_id}` | いいね状態の取得・追加・削除 |

## 11. DBとデプロイ

ローカルはSQLite、本番はPostgreSQLを使用する。接続先は `DATABASE_URL` 環境変数で
切り替える。スキーマ変更はAlembicで管理し、アプリ起動前に
`alembic upgrade head` を実行する。`Base.metadata.create_all()` には依存しない。

## 12. 設計書と実装の差分整理

### A. 実装しながら判断を変えた項目

| 項目 | 更新後の判断 | 変更理由 |
|---|---|---|
| ingredients.memo | 維持する | 実際の利用で補足情報が必要と分かった |
| 投稿への写真選択 | PostCreateの`photo_id`へ統合 | 1投稿1写真のMVPでは専用APIより単純 |
| カレンダーAPI | 投稿一覧をフロントで日付絞り込み | 現在のデータ量では専用APIが不要 |

### B. 単に実装されていない項目

| 項目 | 現在 | 対応方針・期限 |
|---|---|---|
| cooking_post_photos | `photos.post_id` の直接FK | 複数写真要件を確認し、2026-08-31までにPhase2判断 |
| ingredients.quantity | 未実装 | String型での追加を2026-08-31までにPhase2判断 |
| ingredients.storage_place | 未実装 | 選択式での追加を2026-08-31までにPhase2判断 |

### C. 今回の修正で回収した項目

| 項目 | 状態 |
|---|---|
| posts.cooked_date | 実装済み。全日付表示を調理日基準へ統一 |
| photos.content_type | 実装済み。形式・サイズ検証とUUID保存も追加 |

## 13. 学び

設計上の判断は、実装へ反映され、前提条件が運用環境でも成立して初めて意味を持つ。
また、実装中に判断を変えること自体は問題ではなく、その理由を設計書へ戻さないことが
将来の不整合を生む。今後は設計書を実装中のチェックリストとして使い、判断変更と
実装漏れを分けて記録する。
